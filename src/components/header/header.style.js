import { COLORS, FONT_SIZE } from "../../constants/theme";

export const styles = {
    header: {
        width: "100%",
        alignItems: "center"
    },
    logo: {
        width: 200,
        height: 50
    },
    titulo:{
        color: COLORS.dark_gray,
        fontSize: FONT_SIZE.md,
        textAlign: "center"
    }
}