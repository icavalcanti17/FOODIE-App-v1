import { COLORS, FONT_SIZE } from "../../constants/theme";

export const styles = {
  icone: {
    width: 250,
    height: 140
  },
  banner: {
    paddingRight: 20,
    marginTop: 30,
    marginBottom: 15
  }
};
